FROM {{ base_image }}

WORKDIR /app

RUN pip install --no-cache-dir fastapi uvicorn python-multipart{% if dependencies %} {{ dependencies | join(' ') }}{% endif %}

COPY main.py .

EXPOSE 8000

CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]

[project]
name = "helix"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "docker>=7.1.0",
    "httpx>=0.28.1",
    "pytest>=9.0.2",
]
